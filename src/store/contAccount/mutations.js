export const setEdges = (state, edges) => {
  state.edges = edges
}

export const setComponents = (state, components) => {
  state.components = components
}

export const setTreeAccounts = (state, treeAccounts) => {
  state.treeAccounts = treeAccounts
}
